import { ImagesAsyncPipe } from './imagesAsync.pipe';

describe('ImagesPipe', () => {
  it('create an instance', () => {
    const pipe = new ImagesAsyncPipe();
    expect(pipe).toBeTruthy();
  });
});
